<IfModule mod_rewrite.c>
    Options +FollowSymlinks
    RewriteEngine On
</IfModule>

<IfModule mod_rewrite.c>
   # front
   RewriteCond %{REQUEST_URI} ^.*$
   RewriteRule ^(.*)$ /frontend/web/$1
</IfModule>
